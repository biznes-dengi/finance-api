package com.finance.app.account.boundary;

import io.swagger.v3.oas.annotations.tags.Tag;

@Tag(name = "Account", description = "Contains all endpoints related to the account module")
public interface AccountBoundaryMetadata {
}
